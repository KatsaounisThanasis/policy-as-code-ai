# ── Terraform ────────────────────────────────────────────────
terraform/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Keep .terraform.lock.hcl tracked (pins provider versions)

# ── Scan artifacts (regenerated by `make scan` / `make remediate`) ──
.scan/tfplan
.scan/tfplan.json
.scan/main_fixed.tf
.scan/verify_*
# Kept as sample output: .scan/violations.json, .scan/explanations.md

# ── CI artifacts (generated locally when testing the workflow) ──
/violations.json
/policy.sarif

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Team tooling (local agent session state) ─────────────────
.team/

# ── OS / editor ──────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Internal planning/handoff (local only)
NEXT_SESSION.md

# project-cartographer local knowledge graph
.cartographer/
